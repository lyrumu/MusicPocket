# Music Pocket 后续路线图

## 项目阶段

Music Pocket 已具备本地音乐播放器 MVP 的主要能力：导入、资料库、艺术家、歌单、搜索、待播队列、后台播放、系统媒体控制和基础跨平台适配。

下一阶段的目标不是继续堆叠入口，而是把产品推进为：

- 资料可信：不会重复导入、误删或遗留大量孤儿文件。
- 播放稳定：应用界面、实际音频和系统锁屏使用同一份播放状态。
- 整理高效：专辑、排序、筛选、批量操作和歌单编排完整可用。
- 跨端自然：手机与桌面采用适合各自屏幕和输入方式的布局。
- 可长期维护：关键数据链路有测试、迁移、备份和恢复能力。

## 执行原则

1. 严格遵守 `AGENTS.md`，一次只完成一个小功能。
2. 每完成一项后更新 `DONE.md`。
3. 涉及 Drift 表结构、生成模型或 Riverpod 生成代码时运行 `build_runner`。
4. 每项至少运行 `flutter analyze` 和相关测试。
5. 未经用户确认，不读取或使用 `namida/`。

## P0：本地资料可信度

### 任务 1：可靠识别重复导入(已经做过）

当前问题：导入前使用原始 `sourcePath` 查询，但数据库保存的是复制后的应用内部路径，因此同一文件再次导入时可能无法识别为重复。

目标：使用稳定的内容身份识别歌曲，不能依赖复制前后的文件路径一致。

建议方案：

- 为歌曲增加可持久化的内容指纹，例如流式计算 SHA-256。
- 导入前计算指纹并查询数据库；相同内容即跳过。
- 不同路径但内容完全相同的文件也应识别为重复。
- 为已有歌曲补齐指纹，避免升级前的歌曲再次被导入。
- 保留当前导入结果中的成功、跳过、失败统计。

验收标准：

- 同一路径同一文件连续导入两次，第二次为跳过。
- 把同一文件复制到另一路径再导入，仍然为跳过。
- 内容不同但文件名相同的歌曲可以分别导入。
- 旧数据库能正常迁移，已有歌曲不丢失。
- 有 DAO、Repository 或指纹计算相关自动化测试。

### 任务 2：删除应用托管的音频与封面（已经做过）

- 删除歌曲时同步删除应用内部复制的音频文件。
- 仅在封面没有被其他歌曲引用时删除封面。
- 替换或清除自定义封面时清理旧的无引用文件。
- 文件清理失败时不能破坏数据库一致性，并向用户给出结果。

### 任务 3：导入事务、回滚与结果报告

- 采用临时文件写入并原子落盘。
- 复制、封面保存或数据库写入失败时清理残留文件。
- 导入前展示新增、重复、损坏和预计占用空间。
- 导入结束后保留失败文件及具体原因，支持重试。
- 明确告知用户歌曲会复制到 Music Pocket 内部存储。

### 任务 4：资料库维护与缺失文件检测

- 启动或手动维护时检测数据库记录对应的音频是否存在。
- 提供缺失、损坏和孤儿文件报告。
- 支持移除无效记录、重新关联或清理孤儿文件。

## P0：播放核心稳定性

### 任务 5：统一 PlaybackSnapshot

- 建立单一播放状态快照，包含 track/source revision、position、duration、buffered、playing、loading、buffering、seeking 和 error。
- MiniPlayer、完整播放器和系统媒体控制全部使用同一状态来源。
- 不再由各 Widget 分别组合 `positionStream`、`durationStream` 和本地 seek 状态。
- 当前已知的主动 seek 后进度不一致问题在此任务中专项验证。

### 任务 6：串行化播放命令

- 对选曲、加载、上一首、下一首和 seek 增加操作版本或命令队列。
- 过期的异步加载结果不能覆盖最新歌曲。
- 加载成功后才提交当前曲目和播放状态。
- 播放失败进入明确错误状态，并支持重试或跳过。

### 任务 7：补齐 audio_session 与系统媒体同步

- 配置音乐播放 AudioSession。
- 处理系统中断、duck、耳机拔出和音频路由变化。
- 锁屏同步真实 position、bufferedPosition、duration 和 processingState。
- 统一 stop、关闭 MiniPlayer 和清空系统 MediaItem 的语义。

### 任务 8：受控的启动恢复

- 恢复完成前避免用户操作与恢复任务互相覆盖。
- 恢复期间屏蔽错误的 completed 自动切歌。
- 保存位置按真实时长裁剪，接近结尾时采用明确策略。
- 队列状态使用临时文件加原子替换，并能从损坏 JSON 自愈。

### 任务 9：准确记录播放次数

- 不再在 `currentTrackStream` 发出歌曲时立即计数。
- 仅在成功播放达到约定阈值后计入播放次数。
- 恢复旧歌曲、加载失败和快速跳过不应增加次数。

## P1：资料库核心能力

### 任务 10：专辑分类

- 根据 album 与 albumArtist 生成专辑集合。
- 增加专辑网格、专辑详情、播放全部和随机播放。
- 处理未知专辑、合辑和同名专辑边界。

### 任务 11：资料库排序与筛选

- 支持按标题、艺术家、专辑、最近导入、最近播放和最常播放排序。
- 支持按流派、年份等已有元数据筛选。
- 保存用户上次选择的排序方式。

### 任务 12：本地首页与自动集合

- 增加最近导入、最近播放、最常播放和常用歌单。
- 首页只呈现本地内容，不引入服务端或流媒体依赖。
- 在播放统计准确后再实现此任务。

### 任务 13：完善歌单编排

- 修复 `PlaylistDao.updatePlaylist()` 缺少 `where` 的危险更新接口。
- 支持歌单内拖拽排序、批量添加和撤销移除。
- 支持编辑描述、自定义封面和四宫格默认封面。
- 播放全部与随机播放使用明确的并列入口。

### 任务 14：桌面拖放与批量操作

- Mac/Windows 支持拖入文件或文件夹导入。
- 资料库支持多选、批量加入歌单、批量编辑与删除。
- 保留键盘快捷键、右键菜单和清晰的焦点状态。

## P1：导航与界面体验

### 任务 15：保留状态的响应式导航壳

- 使用 `IndexedStack` 或等价结构保留标签、滚动位置和搜索内容。
- 手机使用底部导航，宽屏 Mac/Windows 使用侧栏或 NavigationRail。
- “导入”改为主操作按钮，不再伪装为导航目的地。

### 任务 16：统一产品术语

- 临时播放内容统一称为“待播队列”。
- 用户永久保存的集合统一称为“歌单”。
- 清理“播放列表”“加入播放列表”等容易混淆的文案。

### 任务 17：降低歌曲列表与 MiniPlayer 密度

- 手机歌曲行只保留一个主要操作和“更多”。
- 手机 MiniPlayer 保留封面、歌曲信息、播放和下一首。
- 播放模式、队列管理和关闭等操作移入合适的二级入口。
- 桌面端可以保留更完整的控制。

### 任务 18：建立“私人唱片口袋”视觉识别

- 保持资料库背景克制，把视觉重点留给音乐封面。
- 从当前封面提取主色，仅用于正在播放标记、MiniPlayer 和完整播放器氛围。
- 专辑与歌单使用清晰的方形封面层级，艺术家继续使用头像语义。
- 未提供封面的歌曲使用由标题稳定生成的双色图形。
- 动效集中在切歌与播放状态，不堆叠无意义动画。

### 任务 19：响应式完整播放器

- 手机使用纵向封面与控制布局。
- 桌面宽屏使用“封面在左、信息和待播队列在右”的双栏布局。
- 明确呈现加载、缓冲、文件丢失、播放失败和重试状态。

## P2：设置、备份与维护

### 任务 20：持久化设置

- 使用已有 AppSettings 或专用设置仓储保存主题、音量、默认播放模式和资料库排序。
- 支持跟随系统、浅色和深色三种主题模式。
- 根据平台决定是否展示应用内音量控制。

### 任务 21：资料库导出与恢复

- 支持导出数据库、歌单和用户编辑信息。
- 支持从备份恢复，并处理版本迁移。
- 评估媒体文件“包含在备份中”与“仅导出索引”两种模式。

### 任务 22：M3U 歌单导入导出

- 导出歌单供其他本地播放器使用。
- 导入时报告已匹配、缺失和重复歌曲。

## P2：测试与发布保障

### 任务 23：数据链路测试

- 覆盖重复导入、失败回滚、物理文件删除和孤儿清理。
- 覆盖 Drift v1/v2 到当前版本的迁移。
- 验证外键、级联删除和数据库完整性。

### 任务 24：播放状态机测试

- 抽象可替换的播放器接口。
- 覆盖快速切歌、重复 completed、seek near end、坏文件、队列、历史、随机和恢复。
- 使用短 MP3 VBR、M4A、FLAC、WAV 样本做平台集成验证。

### 任务 25：跨平台格式与发布闸门

- 建立 iOS、macOS、Windows 的实际可播放格式矩阵。
- 导入时 probe 文件是否可播放，而不只检查扩展名。
- 补齐 Apple 签名、隐私说明、备份策略、Windows 安装包与代码签名。
- 在 CI 中运行 analyze、test 和可行的平台构建。

### 任务 26：清理技术债与项目文档

- 修复缺失的 `assets/images/`、`assets/icons/` 警告。
- 清理未使用的模型、表、字段和依赖，或明确其后续用途。
- 更新 `AGENTS.md` 中已经过期的项目现状。
- 补充真正描述 Music Pocket 的 README、开发和发布说明。

## 当前推荐执行顺序

1. 任务 1：可靠识别重复导入。
2. 任务 2：删除应用托管的音频与封面。
3. 任务 3：导入事务、回滚与结果报告。
4. 任务 5：统一 PlaybackSnapshot。
5. 任务 6：串行化播放命令。
6. 任务 10：专辑分类。
7. 任务 11：资料库排序与筛选。
8. 再按实际使用反馈选择其余任务。
